CREATE TABLE orders (
    order_id       BIGSERIAL PRIMARY KEY,
    weight        real,
    region        integer,
    delivery_hours text[],
    cost          integer,
    completed_time timestamp
);

